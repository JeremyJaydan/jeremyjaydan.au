{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tailwind": "tailwindcss -i src/index.css -o static/dist/client.css",
    "esbuild": "esbuild src/index.ts --bundle --minify --outfile=static/dist/client.js",
    "build": "npm run esbuild && npm run tailwind",
    "dev": "nodemon --watch ./index.html --watch ./src --ext html,ts,css --exec 'npm run build'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.24.2",
    "nodemon": "^3.1.9",
    "tailwindcss": "^3.4.17"
  }
}
